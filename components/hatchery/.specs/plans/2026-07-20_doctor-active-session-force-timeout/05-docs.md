# 05. Docs — 文档更新清单

---

## 结论：无需更新文档

本次改动仅涉及后台定时任务 `task/doctor_cleanup.go` 内部的一个兜底超时判断分支：
- 不涉及任何 API 接口新增/修改/删除，`docs/API.md` 无需更新
- 不涉及 GORM model / 数据库 schema 变更，无需更新 `sql/init.sql` 或增量迁移文件
- 不涉及新增用户可见文案，无需更新 `i18n/keys.go` / `i18n/en.go`
- 不涉及对外行为契约变更（对用户而言，效果是"卡死的诊断会话现在能在 12h 内正确释放"，
  与既有文档描述的"12 小时兜底销毁"承诺一致，无需修改文档说明）
