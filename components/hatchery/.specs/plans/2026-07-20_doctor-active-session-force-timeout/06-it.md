# 06. IT — 集成测试

---

## 结论：无新增集成测试

本次改动为纯后台定时任务（`doctor-cleanup`）内部逻辑修复，未新增/修改任何 HTTP API 接口，
`/openclaw/doctor/*` 系列接口的请求参数、响应结构、契约均未变化。现有集成测试
（`test/scripts/openclaw_instance/test_instance_doctor.py`）已覆盖这些接口的契约测试，
无需为本次改动新增 IT 用例。

## 本地验证

已通过单元测试（内存 SQLite）验证定时任务扫描逻辑的正确性，详见
[04-ut.md](./04-ut.md)。后台定时任务的端到端行为（TAT 调用、CVM 销毁）依赖真实云资源，
不适合纳入自动化 IT，遵循现有 `doctor_cleanup_test.go` 的 mock 测试策略。
