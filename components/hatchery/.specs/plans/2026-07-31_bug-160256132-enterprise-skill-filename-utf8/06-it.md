# 06. IT — 集成测试（TAPD 160256132）

## 前置检查

- [x] Docker 可用：`docker info` 返回 0
- [ ] 镜像仓库已登录：未读取本机凭证，无法确认 TCR 登录状态
- [ ] Kubeconfig 可用：存在 `cls` context，但 `kubectl cluster-info --request-timeout=5s` 返回 1
- [ ] AK/SK：本任务未提供指定账号 AK/SK

## 计划运行命令

```bash
make test IMAGE=<镜像> TEST_ARGS="--ak <redacted> --sk <redacted> --run skills"
```

## 执行结果

| # | 用例 | 方法 | 路径 | 结果 |
|---|------|------|------|------|
| 1 | 含非 UTF-8 文件名的企业技能创建应返回明确 400 | POST | `/admin/skills/create` | BLOCKED |

## 失败用例定位

未执行部署/端到端测试。阻塞原因：

1. 当前 kubeconfig context 无法访问集群；
2. 未确认 TCR 登录；
3. 未提供模板指定账号的 AK/SK。

替代验证：

- ZIP 文件名严格校验由 GB18030、Big5、Shift-JIS 和非法字节 race UT 覆盖；
- 合法 UTF-8 输出 ZIP 被重新读取并验证中文条目 `NonUTF8=false`；
- `controller` 全包测试与 `go build ./...` 通过。
