# 04. UT — 单元测试（TAPD 160256132）

## 用例执行结果

| # | Plan 用例 | 测试函数 | 结果 |
|---|----------|---------|------|
| 1 | GB18030、Big5、Shift-JIS 文件名统一拒绝 | `TestValidateSkillZip_RejectsNonUTF8FileNames` | PASS |
| 2 | 合法 UTF-8 缺失 flag | `TestValidateSkillZip_RepairsMissingUTF8Flag` | PASS |
| 3 | 非法字节文件名 | `TestValidateSkillZip_InvalidLegacyFileName` | PASS |
| 4 | 合法 ZIP 回归 | `TestValidateSkillZip_ValidZip` | PASS |
| 5 | 安全校验回归 | `TestValidateSkillZip_BadFileNameChar` 等 | PASS |
| 6 | vet 门禁修复回归 | `TestReadBody` | PASS |
| 7 | 创建接口明确返回 400 | `TestHandleCreateSkill_NonUTF8FileName` | PASS（含 race） |

## 执行命令

```bash
GO111MODULE=on go test ./controller \
  -run 'TestValidateSkillZip(_RejectsNonUTF8FileNames|_RepairsMissingUTF8Flag|_InvalidLegacyFileName|_ValidZip|_BadFileNameChar|_EmptyZip|_NoSkillMD)?$' \
  -race -count=1 -coverprofile=/tmp/clawpro-160256132-cover.out

GO111MODULE=on go test ./controller -count=1
GO111MODULE=on go test ./controller -run 'TestHandleCreateSkill_NonUTF8FileName$' -race -count=1
GO111MODULE=on go test ./skillhubclient -count=1
GO111MODULE=on go vet ./...
GO111MODULE=on go build ./...
```

结果：

- 定向 `validateSkillZip` race 测试：PASS
- 创建接口非 UTF-8 响应 race 测试：PASS
- `controller` 全包测试：PASS，192.702 秒
- `skillhubclient`：PASS
- 全仓 `go vet ./...`：PASS
- 全仓 `go build ./...`：PASS

## 覆盖率

| 包/函数 | 覆盖率 |
|---------|--------|
| `controller.validateSkillZip` | 83.8% |
| 定向测试对应 `controller` 包总语句 | 0.2%（包体量大，仅运行目标用例） |

## 未覆盖行

- `validateSkillZip` 的 ZIP 读取/写入底层异常分支依赖损坏流或 I/O 注入，沿用既有测试覆盖策略。
- `HandleCreateSkill` 的真实 SMH 上传未在 UT 中执行，由 IT 环境负责。
