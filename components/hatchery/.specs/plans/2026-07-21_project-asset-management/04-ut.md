# UT

## 覆盖范围

- 项目管理：新增 `controller/admin_projects_test.go`，覆盖列表、搜索、计数、创建、重名、更新、删除影响、删除阻塞、成员查询及成员增删改。
- 项目应用范围：覆盖技能/规范共用的可见范围解析；`project_ids` 为可选参数，分组与项目范围按请求整体 replace。
- Local Agent：测试库补齐项目、分组 catalog、scope binding 等依赖，覆盖 Workspace `project_id` 与用户级分组资产路径。
- 资产版本：既有 `controller/asset_version_test.go` 覆盖版本记录、segments、同步模式与自动触发链路。

## 执行结果

2026-07-21 执行：

```text
go test ./controller -count=1
```

结果：通过。

## 说明

- 未执行 `go test ./... -race`：本期工作区的控制器与模型相关改动已通过定向回归；完整 race 套件留由 CI 执行。
- 远程 HTTP 场景记录于 [06-it.md](./06-it.md)。
