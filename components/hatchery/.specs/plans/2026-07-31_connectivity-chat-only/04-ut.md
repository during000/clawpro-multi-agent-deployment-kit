# 04. UT — 单元测试

> 用例执行结果、覆盖率、未覆盖行。

---

## 一、测试执行结果

### 改动相关测试（已验证通过）

```bash
go test ./controller/ -run 'TestHandleAdminModelConnectivity|TestHandleModelConnectivity|TestProvider_CheckConnectivityWithChat|TestOpenAICheckConnectivityWithChat|TestAnthropicCheckConnectivityWithChat' -v -race -count=1
```

结果：**全部 PASS**

| 测试用例 | 结果 |
|---------|------|
| `TestHandleAdminModelConnectivity_TemporaryCredentialsSuccess` | PASS |
| `TestHandleAdminModelConnectivity_SavedModelSuccess` | PASS |
| `TestHandleAdminModelConnectivity_SavedModelFailureReturnsDetails` | PASS |
| `TestHandleAdminModelConnectivity_UpstreamStatusClassification` (4 子用例) | PASS |
| `TestHandleAdminModelConnectivity_AnthropicTemporaryCredentialsSuccess` | PASS |
| `TestHandleAdminModelConnectivity_ChatProbeDirectHit` | PASS |
| `TestHandleAdminModelConnectivity_NoModelReturns400` | PASS |
| `TestHandleAdminModelConnectivity_ChatProbeFail` | PASS |
| `TestHandleAdminModelConnectivity_AnthropicChatProbeDirectHit` | PASS |
| `TestHandleModelConnectivity_TemporaryCredentialsSuccess` | PASS |
| `TestHandleModelConnectivity_AnthropicTemporaryCredentialsSuccess` | PASS |
| `TestHandleModelConnectivity_SavedModelVisibilityAllowed` | PASS |
| `TestProvider_CheckConnectivityWithChat_InterfaceCompliance` | PASS |
| `TestOpenAICheckConnectivityWithChat_SSRFBlocked` | PASS |
| `TestAnthropicCheckConnectivityWithChat_SSRFBlocked` | PASS |

```bash
go test ./controller/provider/ -v -race -count=1
```

结果：**全部 PASS**

### 编译与静态检查

```bash
go build ./controller/... ./controller/provider/...   # ✅ 通过
go vet ./controller/... ./controller/provider/...      # ✅ 通过
gofmt -l <所有改动文件>                                 # ✅ 无格式问题
```

---

## 二、覆盖率说明

本次改动为**删除代码**（移除 list-models 探活路径及其实现），新增代码极少（仅 `handleModelConnectivity` 中 2 行直调 + `probe` 变量简化）。

- `CheckConnectivityWithChat` 的所有测试用例保持不变，覆盖率不受影响
- `handleModelConnectivity` 的测试覆盖了：成功路径、失败路径、无 model 返回 400、SSRF 阻断
- 删除的 `CheckConnectivity` 方法不再需要覆盖率

---

## 三、未覆盖行

无新增未覆盖行。
