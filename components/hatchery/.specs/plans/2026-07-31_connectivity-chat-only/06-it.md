# 06. IT — 集成测试

> 构建镜像、K8s 部署、端到端验证、增量覆盖率。

---

## 集成测试评估

本次改动**不涉及**新增 API 接口或请求参数变更：

- `POST /admin/models/connectivity` 和 `POST /openclaw/models/connectivity` 的请求/响应格式不变
- 仅后端探活逻辑从 "list-models 优先 + chat 回退" 改为 "chat 直接探活"
- 已有的集成测试用例（如果存在）无需修改，因为外部行为（接口契约）未变

因此本次改动**无需新增集成测试用例**。

---

## 构建验证

```bash
go build ./...   # ✅ 通过
```

---

## 增量覆盖率

本次改动为删除代码 + 极少量新增，单元测试已覆盖所有改动路径。集成测试层面的增量覆盖率不适用（无 API 契约变更）。
