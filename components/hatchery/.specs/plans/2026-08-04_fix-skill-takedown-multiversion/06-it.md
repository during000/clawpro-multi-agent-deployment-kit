# 06. IT — 集成测试

> 集成测试：脚本补齐 + 本地/CI 验证说明。本步骤不改业务逻辑。

---

## 一、脚本改动

| 文件 | 改动 |
|------|------|
| `test/scripts/skill/test_skill_contribute.py` | 新增 **IT-M1** 多版本下架闭环 |
| `test/scripts/helpers/contribution.py` | `skillstore_has_slug` 优先走 `/openclaw/skillstore/detail` |

### IT-M1 步骤

1. contribute `1.0.0` → approve → published  
2. contribute `1.0.1` → approve → 列表最新为 1.0.1  
3. takedown  
4. `GET /admin/skills?slug=` 最新行 `pending_review.action_type=takedown` 非空  
5. approve → status=offline，skillstore detail 404  

对应线上 bug（diagram-imagexxx）回归。

---

## 二、本地已执行验证

| # | 验证项 | 结果 |
|---|--------|------|
| 1 | UT 多版本 5/5（04-ut） | PASS |
| 2 | `go build ./controller/` | PASS |
| 3 | 远程 E2E 跑 IT-M1 | **待部署本分支后再跑**（当前测试环境仍为未修复镜像，跑 IT-M1 会复现旧 bug） |

远程完整脚本执行命令（部署含本修复的镜像后）：

```bash
cd test/scripts
export API=http://<hatchery>/api
export ADMIN_TOKEN=<seed-admin-token>
python3 skill/test_skill_contribute.py
```

CI：`make test` 会自动发现 `test/scripts/skill/test_skill_contribute.py`。

---

## 三、覆盖对照

| Plan | 状态 |
|------|------|
| IT-M1 多版本下架闭环 | ✅ 脚本已落地 |
| IT-M2 旧 resource_id 兼容 | ✅ 由 UT-T2 覆盖（IT 不强制造旧 id） |

---

## 四、风险

| # | 风险 | 缓解 |
|---|------|------|
| 1 | 未部署修复前远程 IT-M1 失败 | 先合入/部署本分支再跑；UT 已覆盖核心断言 |
| 2 | SMH 未开 | 脚本 `require_smh_enabled` → SKIP exit 0 |
