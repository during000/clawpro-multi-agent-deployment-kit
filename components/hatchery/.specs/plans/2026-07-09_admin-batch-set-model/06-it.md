# 06. IT — 集成测试

> 包含构建镜像、K8s 部署、端到端 API 验证、增量覆盖率。详见 `test/README.md`。
> 依赖 Docs 步骤产出的 `docs/API.md` 生成 OpenAPI spec。

---

## 前置检查

> 按 `CODEBUDDY.md` / `test/README.md` 的 IT 流程执行；凭证只通过环境变量传入，不写入本文档。

- [x] OpenAPI 当前分支与基线 spec 已生成：`make openapi BASE_BRANCH=origin/master`
- [x] Kubeconfig 可用：用户指定 `KUBECONFIG=/Users/yutaoguo/Downloads/cls-m29jw7bu-config`
- [x] AK/SK 可用：`../.env.local` 中存在 `TENCENTCLOUD_SECRET_ID` / `TENCENTCLOUD_SECRET_KEY`
- [x] K8s 集成测试编排已进入真实集群部署与 cleanup 阶段
- [ ] 本机 Docker 可用：`docker info` 失败；未启动本机 Docker runtime

---

## 已执行命令

### 1. OpenAPI 当前分支 + 基线生成

```bash
make openapi BASE_BRANCH=origin/master
```

结果：PASS。

```text
当前分支：
  Found 381 endpoint definitions
  Unique paths: 363
  Total operations: 373

基线分支：
  Found 380 endpoint definitions
  Unique paths: 362
  Total operations: 372
```

### 2. 本机 Docker daemon 检查

```bash
docker info
```

结果：FAIL。

```text
Server:
failed to connect to the docker API at unix:///var/run/docker.sock; check if the path is correct and if the daemon is running: dial unix /var/run/docker.sock: connect: no such file or directory
```

### 3. Kubeconfig 检查

```bash
kubectl --kubeconfig /Users/yutaoguo/Downloads/cls-m29jw7bu-config cluster-info
```

结果：PASS。

```text
Kubernetes control plane is running at https://119.91.188.59:443
CoreDNS is running at https://119.91.188.59:443/api/v1/namespaces/kube-system/services/kube-dns:dns-tcp/proxy
```

### 4. K8s 集成测试

> 用户要求 kubeconfig 使用 `/Users/yutaoguo/Downloads/cls-m29jw7bu-config`。本机 Python 为 3.9，首次本机执行因测试脚本使用 `str | None` 类型标注而在 import 阶段失败；随后在 devcloud 上运行测试脚本，避免本机 Python 版本问题。

```bash
make test \
  IMAGE=keitewang-test.tencentcloudcr.com/ngc/hatchery:admin-batch-set-model-20260709160000 \
  BASE_BRANCH=origin/master \
  TEST_ARGS="--report-dir ./test-report --ak $TENCENTCLOUD_SECRET_ID --sk $TENCENTCLOUD_SECRET_KEY --kubeconfig /tmp/hatchery-it.pf2XpC/cls-m29jw7bu-config --namespace $TARGET_NAMESPACE"
```

结果：FAIL（完整套件非全绿），但新增接口脚本 PASS。

```text
Python 3.11.6
Found 381 endpoint definitions
Total operations: 373
Found 380 endpoint definitions
Total operations: 372
PASS  test_batch_set_model.py
17/96 test scripts failed
Coverage: merged 1869 frames from 79 scripts → test-report/coverage-data.json
Coverage: using base spec for incremental analysis: /tmp/hatchery-it.pf2XpC/docs/openapi_base.json
Coverage: HTML report generated → test-report/coverage.html
Running cleanup script...
All instances gone
All security groups deleted
Deleting StatefulSet: hatchery-51c83536
Deleting Service: hatchery-51c83536
Report generated: test-report/index.html
make: *** [Makefile:52: test] Error 1
```

---

## 执行结果

| # | 用例 | 方法 | 路径 | 结果 |
|---|------|------|------|------|
| 1 | OpenAPI 当前 + 基线生成 | make | `make openapi BASE_BRANCH=origin/master` | PASS |
| 2 | 本机 Docker daemon 前置检查 | docker | `docker info` | FAIL：本机 daemon 未运行 |
| 3 | Kubeconfig 前置检查 | kubectl | `kubectl cluster-info` | PASS |
| 4 | 完整 K8s 集成测试 | make | `make test IMAGE=... BASE_BRANCH=origin/master TEST_ARGS="--report-dir ..."` | FAIL：17/96 脚本失败 |
| 5 | 新增批量模型接口脚本 | Python IT | `test/scripts/model/test_batch_set_model.py` | PASS |
| 6 | K8s / 云资源 cleanup | integration runner | cleanup phase | PASS：实例、SG、StatefulSet、Service 均进入删除流程并完成 |

---

## 失败用例定位

完整套件失败集中在通道和模型生命周期类外部依赖用例；新增接口脚本 `test_batch_set_model.py` 已通过。

失败脚本摘要：

- channel：`test_ddingtalk_channel.py`、`test_feishu_channel.py`、`test_qq_channel.py`、`test_wecom_*` 等。
- model：`test_bind_preset_model.py`、`test_custom_model.py`、`test_default_model_bind.py`、`test_fallback_switch.py`、`test_model_lifecycle.py`、`test_model_lifecycle_hermes.py`、`test_model_update_sync_instances.py`。
- 新增脚本：`test_batch_set_model.py` PASS。

用户确认：上述外部依赖失败项直接跳过，不阻塞后续 Review / Commit。

---

## 结论

- IT 已按 `make openapi BASE_BRANCH=origin/master` + `make test IMAGE=... BASE_BRANCH=origin/master TEST_ARGS="--report-dir ..."` 路径执行到真实 K8s 部署、脚本运行、覆盖率报告生成和 cleanup。
- 完整 IT 不是全绿，退出码为 2；外部依赖失败项按用户确认跳过。
- 本次新增 API 的集成脚本 `test_batch_set_model.py` 通过。
- Review 阶段补充了批量 TAT 部分成功后的 CVM best-effort 回滚逻辑；该逻辑不再使用 devcloud Docker 重建镜像，覆盖证据记录在 `04-ut.md` / `07-review.md`。
