---
task_id: hermes-agent-upgrade
stage: 04-ut
author: AI（反向推导）
date: 2026-07-20
run_mode: manual
---

# UT（反向补建）

## 执行命令

```bash
go build ./...
go test ./controller/... -run 'Hermes|Upgrade|Restore|Backup' -count=1 -v
```

## 结果

- `go build ./...`：**通过**，无编译错误。
- 升级相关测试（`controller/openclaw_upgrade_hermes_test.go` 等）：**全部通过**。
- 核心覆盖用例（对照 02-plan.md 测试用例设计 1-6 项）：均有对应测试函数覆盖。

## 覆盖率

未单独跑增量覆盖率脚本（`BASE_BRANCH=origin/master bash .ci/ci-check-coverage.sh`），建议在提 MR 前补跑一次，作为 Commit 前最后确认项。

## 已知不足

- 未发现针对 `config/agent_plugin_preserve_paths.json` 与脚本内 `PRESERVE_PATHS` 一致性的测试（Review 阶段已补充，见 07-review.md）。
