# 05. Docs — 文档更新

---

## 更新清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `docs/API.md` | 修改 | discord 通道参数说明新增 `user_id`（用户 ID） |

## 具体变更

`docs/API.md` 第 2959 行：

```
- **discord**: `bot_token`（机器人 Token）、`user_id`（用户 ID）
```

## 检查项

- [x] `docs/API.md` 已更新（discord 参数新增 user_id）
- [x] 无新增 API 接口，无需更新接口签名
- [x] 参数表格式符合 CLAUDE.md 要求
