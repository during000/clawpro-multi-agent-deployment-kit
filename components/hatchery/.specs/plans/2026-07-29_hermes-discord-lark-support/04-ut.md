# 04. UT — 单元测试

---

## 用例执行结果

> 与 Plan §测试用例 逐条对齐

| # | Plan 用例 | 测试函数 | 结果 |
|---|----------|---------|------|
| 1 | Hermes 通道列表包含 discord | `TestSupportedChannelsByAgentType_Contents/hermes-supported-channels` | PASS |
| 2 | Hermes 通道列表包含 lark | `TestSupportedChannelsByAgentType_Contents/hermes-supported-channels` | PASS |
| 3 | Hermes 通道总数为 9 | `TestSupportedChannelsByAgentType` (agent_type_extended_test.go:386) | PASS |

## 执行命令

```bash
go test ./model/... -v -race -run "TestSupportedChannelsByAgentType"
```

## 执行结果

```
--- PASS: TestSupportedChannelsByAgentType (0.01s)
--- PASS: TestSupportedChannelsByAgentType_AllTypes (0.01s)
--- PASS: TestSupportedChannelsByAgentType_Contents (0.01s)
    --- PASS: TestSupportedChannelsByAgentType_Contents/openclaw-has-all-known-channels (0.00s)
    --- PASS: TestSupportedChannelsByAgentType_Contents/hermes-supported-channels (0.00s)
    --- PASS: TestSupportedChannelsByAgentType_Contents/ace-only-three (0.00s)
    --- PASS: TestSupportedChannelsByAgentType_Contents/empty-agent-type-as-openclaw (0.00s)
    --- PASS: TestSupportedChannelsByAgentType_Contents/unknown-agent-type-returns-nil (0.00s)
PASS
```

## 修复的测试

合并后发现两处测试断言未更新：

1. `model/agent_type_new_test.go:253`：`wantAny` 缺少 `discord`（lark 分支只加了 `lark`）→ 已补充
2. `model/agent_type_extended_test.go:386`：hermes 通道数断言为 7 → 已改为 9

## 覆盖率

本次改动仅涉及 map 数据变更和 shell 脚本，无新增 Go 逻辑分支，覆盖率无变化。

## 未覆盖行

- shell 脚本（set_channel_hermes.sh / del_channel_hermes.sh）无单测覆盖，依赖集成测试和人工验证
