# 06. IT — 集成测试

> 包含构建镜像、K8s 部署、端到端 API 验证、增量覆盖率。详见 `test/README.md`。
> 依赖 Docs 步骤产出的 `docs/API.md` 生成 OpenAPI spec。

---

## 前置检查

> 开始前逐项确认，缺一项即无法运行集成测试。

- [ ] Docker 可用：`docker info` 正常
- [ ] 镜像仓库已登录：`docker login` 完成
      （凭证获取：https://console.cloud.tencent.com/tcr/repository/tcr/tcr-4531z75r/cvm-openclaw/hatchery/1/tagList →「仓库信息」tab）
- [ ] Kubeconfig 可用：`kubectl --kubeconfig <path> cluster-info` 正常
      （下载：https://console.cloud.tencent.com/tke2/cluster/sub/list/basic/info/apiserver?rid=1&clusterId=cls-m29jw7bu，放到 `~/.kube/config` 或设 `KUBECONFIG`）
- [ ] AK/SK：已通过 `--ak`/`--sk` 传入
      （必须用 3205597606 账号，其他账号未加白名单）

## 运行命令

```bash
make test IMAGE=<镜像> TEST_ARGS="--ak xxx --sk xxx --run <模块>"
```

## 执行结果

> ⚠️ **本地受阻**：本环境无 Docker / K8s / 3205597606 账号 AK-SK / 镜像仓库登录，无法运行真环境端到端联调（SOP 前置检查缺多项）。以下为本地可执行的等价验证。

| # | 验证项 | 命令/范围 | 结果 |
|---|------|------|------|
| 1 | gofmt 格式检查 | 本次改动 8 个文件 | PASS（已自动格式化） |
| 2 | go vet 静态检查 | `go vet ./controller/` | PASS |
| 3 | 单元测试（local agent + delete 相关） | `go test ./controller/ -run "LocalAgent\|UninstallTeamai\|...\|DeleteInstance"` | PASS（13+ 用例全绿） |
| 4 | 增量覆盖率（近似） | 仅 local-agent 用例覆盖文件测算 | 45.8%（偏低，因分母混入非本次重构的累积 diff 行；CI 全量口径以 CI 为准） |
| 5 | 真环境 K8s 部署 + 端到端 | `make test IMAGE=...` | ❌ 本地无资源，未执行 |

## 失败用例定位

- 无（本地可执行项全部通过；真环境 IT 待资源就绪后由 CI/人工执行）
