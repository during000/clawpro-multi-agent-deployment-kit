# 06. IT — 集成测试

> Resource Config Policy 的隔离镜像构建、EKS 部署、真实腾讯云端到端验证与增量 API 覆盖率证据。

---

## 1. 结论

**PASS。** I01–I12 全部通过：

- 3 个目标 Python 脚本在同一次隔离 EKS 流水线中全部 PASS；
- 站点/options 6/6、用户组策略 1/1、实例创建与镜像容量 4/4；
- 真实创建 3 台测试 CVM，分别覆盖镜像容量等于磁盘、小于磁盘、`ImageSize=0`，均通过腾讯云 `DescribeInstances` 反查最终机型、系统盘类型与容量，随后完成销毁；
- `ImageSize=100`、`DiskSize=50` 在云 API/安全组路径前 400，实例列表、占位记录和 SG 池快照均无变化；
- current/base OpenAPI 增量 5/5 operation、6/6 既有 operation 新参数全部覆盖；资源 options 的 `zone`、`instance_charge_type`、`instance_type`、`refresh` 以及创建请求 `resource_config` 均有覆盖帧；
- 测试 Pod 无重启；最终隔离 StatefulSet、Service、CVM、测试 SG、测试用户、测试组、站点配置和镜像 `ImageSize` fixture 全部清理/恢复。

---

## 2. 时间记录

| 项目 | 时间 |
|------|------|
| 开始时间 | 2026-07-19 15:34:50 |
| 结束时间 | 2026-07-19 16:40:36 |
| 耗时 | 01:05:46 |

---

## 3. 前置检查

| 检查项 | 结果 | 证据/说明 |
|--------|------|-----------|
| 当前分支 | PASS | `feat/resource-config-policy` |
| 本地 Docker daemon | N/A | 本机 `/var/run/docker.sock` 不存在；按 `hatchery-deploy` skill 改用远端 Docker host，不阻塞构建 |
| 远端 Docker host | PASS | `yutaoguo-any5.devcloud.woa.com:36000` SSH `BatchMode` 成功 |
| TCR 登录 | PASS | 临时凭证仅通过 stdin 使用；push 完成后已执行 remote `docker logout`，凭证未写入仓库/文档 |
| Kubeconfig | PASS | 目标 TKE control plane 可访问；具体路径不记录到产物 |
| 测试 namespace | PASS | `clawpro-test` |
| 腾讯云凭证 Secret | PASS | `clawpro-test/tencent-cloud-credentials` 存在；测试命令不传/不打印 AK、SK |
| 隔离策略 | PASS | runner 随机后缀生成独立 StatefulSet/Service/label/identifier/空 DB；最终成功 run suffix 为 `ad3b06c0` |
| Python 版本 | PASS | 实际 runner 使用 Python `3.9.6`；目标脚本及 helpers 可导入 |
| OpenAPI current/base | PASS | current 390 operations；`origin/Release/2026_07_15` base 385 operations |

### 安全处理

- 用户提供的 TCR 临时密码未写入任何仓库文件、SOP 文件或测试报告；只通过进程环境与 `--password-stdin` 传给远端 Docker。
- 测试以 `QUIET=1` 运行，避免 `/admin/config` 中的 credential 字段进入帧日志；脚本额外使用 `trace=False` 的 client 读取该接口。
- 测试脚本从 runner 环境接收短生命周期 bootstrap admin token，仅用于未包裹 `WithOpenAPI` 的 `/admin/cloud/query/cvm` 反查；ApiClient 始终对 Authorization 脱敏。
- kubeconfig、AK、SK 和 registry password 均未进入本文件、HTML 报告或 coverage JSON。

---

## 4. 镜像构建与发布

### 4.1 构建方式

按 `skill://hatchery-deploy`：

1. 在远端 Docker host 创建临时目录；
2. `rsync` 当前 worktree（排除 `.git`、构建产物、coverage/report 和 `__pycache__`）；
3. 远端 `docker build`；
4. push 到 TCR；
5. 删除远端临时目录并 logout。

### 4.2 镜像证据

| 项 | 值 |
|----|----|
| Image | `keitewang-test.tencentcloudcr.com/ngc/hatchery:it-resource-config-policy-ceaa1a6-20260719075047` |
| Build image ID | `09dc47e141ca` |
| Registry digest | `sha256:8c883b0008fe6387f82ea6cfc8846626db6495f45a8e717fcd8d2f174c1a80af` |
| Push | PASS |
| Remote temp workspace cleanup | PASS |
| Remote registry logout | PASS |

镜像由当前 worktree 构建，包含 Implement/UT/Docs 阶段尚未进入 Commit 的生产代码；测试脚本在本地 runner 执行，不依赖镜像内 Python 文件。

---

## 5. IT 产物

### 5.1 新增脚本

| 脚本 | 覆盖 |
|------|------|
| `test/scripts/admin_resource_config/test_resource_config.py` | I01–I06：5 个新 operation 鉴权、站点闭环、兼容/校验、i18n basic、真实 CVM/CBS options |
| `test/scripts/admin_user_groups/test_resource_config_policy.py` | I07：组创建、policy set、groups 查询、overview local、delete、site fallback、组清理 |
| `test/scripts/openclaw_instance/test_exclusive_instance_create_resource_config.py` | I08–I11：三层覆盖、真实 CVM 反查、镜像容量三分支、无副作用断言、销毁与 fixture 恢复 |

### 5.2 测试框架兼容修复

| 文件 | 修复 | 原因 |
|------|------|------|
| `test/scripts/helpers/client.py` | 添加 `from __future__ import annotations` | runner 实际 Python 3.9；原 PEP 604 `str | None` 在 import 时抛 `TypeError` |
| `test/scripts/helpers/instance.py` | 添加 `from __future__ import annotations` | 同上；`InstanceContext` 含 PEP 604 类型注解 |
| `test/integration/main.go` | 向脚本环境注入短生命周期 `BOOTSTRAP_ADMIN_TOKEN` | `/admin/cloud/query/*` 未包裹 `WithOpenAPI`，普通管理员 API token 无法用于真实 CVM 反查；bootstrap token 只存在于子进程环境 |

`test/integration` 已执行 gofmt；nested module `go test ./... -count=1` PASS。5 个 Python 文件 `python3 -m py_compile` PASS；Python 3.9.6 实际 import PASS。

---

## 6. 最终运行命令（凭证脱敏）

```bash
make test \
  IMAGE=keitewang-test.tencentcloudcr.com/ngc/hatchery:it-resource-config-policy-ceaa1a6-20260719075047 \
  BASE_BRANCH=origin/Release/2026_07_15 \
  TEST_ARGS="--run '*resource_config*' --concurrency 1 --script-timeout 45m --timeout 90m --report-dir ./test-report/resource-config-policy"
```

运行环境另设：

- `KUBECONFIG=<local path>`（不记录实际值）
- `QUIET=1`
- `NO_COLOR=1`
- `IT_NAMESPACE=clawpro-test`
- `IT_CONTAINER=hatchery`

未向命令行传入 AK/SK；StatefulSet 继续引用 namespace 内既有 Secret。

---

## 7. I01–I12 执行结果

| # | 场景 | 执行证据 | 结果 |
|---|------|----------|------|
| I01 | 5 个资源 operation 鉴权 | GET/POST resource、basic、instance-types、system-disks 分别执行无 token、坏 token、普通 token；均为 401/403；后续管理员正常请求均 200 | PASS |
| I02 | 站点配置读写闭环 | 保存原 `resource_config`/source 和 `cvm_template`；POST 后 GET 为 canonical 大写枚举、`source=site_config`；`cvm_template` byte-for-byte 不变；finally 恢复原值/source | PASS |
| I03 | 未知字段兼容与已知字段校验 | wrapper/top-level/nested 未知字段被忽略且不持久化；非法机型、磁盘类型、49GB、缺 prepaid、period=0 均 400；每次失败后原配置不变 | PASS |
| I04 | basic 中英文 | 固定 charge/prepaid/renew/internet 枚举完整，`source=static`；`Accept-Language` 中英文 description 至少一项不同 | PASS |
| I05 | instance options | 真实 `ap-guangzhou-6/7` 探测，传 `zone`、`instance_charge_type=POSTPAID_BY_HOUR`、`refresh=1`；仅返回 allowlist SELL，CPU/memory 合法 | PASS |
| I06 | system disk options | 真实 CVM family + CBS quota；传 `zone`、charge、`instance_type`、`refresh=1`；类型 allowlist、min≥50、max≥min、step≥0 | PASS |
| I07 | 组策略闭环 | 唯一组创建；set canonical `{"value":{...}}`；groups 查询只保留规范字段；overview `source=local`；delete 后 binding 消失、overview `source=site_default`；组清理 | PASS |
| I08 | 三层用户创建覆盖 | 站点/组/用户使用 3 个不同且符合 CBS step 的磁盘容量；普通用户带 group_id 和 form `resource_config` 创建；腾讯云 `DescribeInstances` 反查最终 InstanceType/SystemDisk 等于用户值；实例销毁 | PASS |
| I09 | 100GB 镜像 + 50GB 盘 | 隔离 SQLite fixture 设 `ImageSize=100`；响应 400 同时含 100/50；用户实例列表前后相同、无目标 placeholder；`managed_sg_pool` 快照不变；同 trace 无 `Call sdk api`/`CVM RunInstances`/`[sg-pool]` | PASS |
| I10 | 镜像等于/小于磁盘 | 分别设置 ImageSize=最终 DiskSize、ImageSize=DiskSize-1；两次均真实 RunInstances 成功，DescribeInstances 反查磁盘未变；等待 running 后销毁并等 destroyed | PASS |
| I11 | `ImageSize=0` | fixture 设 0；真实 RunInstances 成功；DescribeInstances 反查最终磁盘类型/容量保持用户值；等待 running 后销毁 | PASS |
| I12 | 增量 API 覆盖 | 5/5 new operations；6/6 existing-operation new params；options 查询参数与 `resource_config` 等 required 参数集合无 missing | PASS |

---

## 8. 最终流水线统计

### 8.1 脚本结果

| 脚本 | 用例/请求统计 | 结果 |
|------|---------------|------|
| `admin_resource_config/test_resource_config.py` | 6 tests；33 HTTP；33 成功 / 0 失败 | PASS |
| `openclaw_instance/test_exclusive_instance_create_resource_config.py` | 5 tests（含 fixture setup）；75 HTTP；75 成功 / 0 失败 | PASS |
| `admin_user_groups/test_resource_config_policy.py` | 4 tests；8 HTTP；8 成功 / 0 失败 | PASS |
| runner | 3/3 scripts；Pod restart 0 | PASS |

覆盖收集器合并 **138 frames / 3 scripts**。帧数大于 run_tests 摘要请求数，因为还包括每个脚本的 preflight、finally restore/cleanup 和复杂流程外围请求。

### 8.2 API 增量覆盖

`compute_incremental(current, origin/Release/2026_07_15, coverage)`：

| 指标 | 结果 |
|------|------|
| New operations | 5 |
| New operations covered | 5 |
| `new_ops_uncovered` | `[]` |
| Existing-operation new params | 6 |
| Existing-operation new params covered | 6 |
| Unmatched coverage frames | 0 |

5 个新增 operation：

- `GET /admin/config/resource`
- `POST /admin/config/resource`
- `GET /admin/config/resource/options/basic`
- `GET /admin/config/resource/options/instance-types`
- `GET /admin/config/resource/options/system-disks`

6 个既有 operation 新参数：

- `POST /openclaw/create`: `resource_config`
- `POST /admin/group-config/policy`: `group_id`, `config_key`, `value_json`
- `POST /admin/group-config/policy/delete`: `group_id`, `config_key`

额外 required 参数断言：

- instance-types：`zone`, `instance_charge_type`, `refresh`
- system-disks：`zone`, `instance_charge_type`, `instance_type`, `refresh`
- site POST：`resource_config`
- ordinary create：`resource_config`

全部覆盖。

---

## 9. 报告产物

| 文件 | 内容 |
|------|------|
| `test-report/resource-config-policy/index.html` | 最终 3 脚本测试报告 |
| `test-report/resource-config-policy/coverage.html` | current/base API 覆盖率与增量章节 |
| `test-report/resource-config-policy/coverage-data.json` | 138 条覆盖帧 |
| `test-report/resource-config-policy/admin_resource_config/test_resource_config.html` | I01–I06 明细 |
| `test-report/resource-config-policy/admin_user_groups/test_resource_config_policy.html` | I07 明细 |
| `test-report/resource-config-policy/openclaw_instance/test_instance_create_resource_config.html` | I08–I11 明细 |

报告目录为本地测试产物，是否被 gitignore 不影响 SOP 证据；SOP 真相源记录可复现命令、镜像 digest 和最终统计。

---

## 10. 失败定位与修复记录

IT 过程中没有把失败结果当作通过；逐项修复后重新部署验证。

| 次序 | 失败 | 根因 | 修复/验证 |
|------|------|------|-----------|
| 1 | 3 个脚本 import 即失败 | Python 3.9 运行时求值 PEP 604 类型注解 | helpers 加 postponed annotations；Python 3.9.6 实际 import PASS |
| 2 | 创建分组用户返回“用户名不能为空” | 旧 `admin_create_user` helper 在 JSON 模式仍把 `instance_quota` 发送为 string，body decode 整体失败 | 目标脚本直接发送契约正确的 JSON int + `group_ids`；真实 API 200 |
| 3 | I09 SG 快照 SQL 报表不存在 | GORM 自定义表名为 singular `managed_sg_pool` | SQL 改为真实表名；I09 PASS |
| 4 | DescribeInstances 返回 401 | cloud proxy 未使用 `WithOpenAPI`，普通管理员 API token 不适用 | runner 注入 ephemeral bootstrap token；真实 DescribeInstances 200 |
| 5 | 创建成功后立即 user delete 返回 409 | 实例仍处于 create operation | 先等待 running；异常时 admin delete fallback；最终 3 台实例均 destroyed |
| 6 | 一次失败 run 留下运行中 CVM/SG | 脚本在 delete 409 后退出，runner cleanup 5 分钟内未等到云资源消失 | 使用同账号 SDK 显式 Terminate 并轮询不存在，再删除测试 SG；最终确认 instance 不存在、SG 删除；后续脚本清理逻辑修正并在最终 run 验证 |

最终成功流水线不包含上述失败；本节保留真实诊断链，避免掩盖 IT 期间发现的测试基础设施问题。

---

## 11. 清理与恢复证据

最终成功 run `ad3b06c0`：

- 三个真实创建实例均在各自测试中等待 `running`，提交 delete，并等待 `destroyed`；runner cleanup 随后输出 `No instances to terminate`。
- 隔离测试 SG `clawpro-sg-ad3b06c0-hatchery-test-sg-01` 删除成功。
- target scripts 的 finally：
  - 站点 `resource_config` 与 source 恢复；`cvm_template` 未变化；
  - 用户组 resource policy 删除，组删除；
  - 测试用户 hard delete；
  - enabled image `image_size` 恢复为原值；
  - 无实例/组残留。
- runner 删除 `hatchery-ad3b06c0` StatefulSet 与 Service；随后 K8s `get` 分别返回 NotFound，二次确认资源不存在。
- Pod restart check：0。

---

## 12. 非阻塞说明

- 本机 Python 3.9.6 的 urllib3 输出 LibreSSL compatibility warning；不影响 HTTP、脚本结果或覆盖率。未通过 suppress warning 掩盖。
- 本机 Docker daemon 未启动；构建已按部署 skill 使用远端 Docker host 完整替代，build/push/digest 均有证据。
- 本阶段只运行本功能 3 个目标集成脚本，没有把仓库全部历史 IT 脚本纳入本次验收；范围与 Plan I01–I12 一致。

---

## 2026-07-22 资源策略实体化重设计 IT

> 本节验证最终独立 `ResourcePolicy` 设计，并覆盖本文前述站点/分组内嵌资源配置版本的 IT 结论；冲突时以本节为准。

### 结论

**PASS。** I01–I12 全部通过：

- 同一次隔离 EKS run 中 3/3 目标脚本通过，15 tests、116 HTTP 请求全部成功；
- 四个策略管理 API、两个动态 options、用户组树直接策略、继承/默认回退及用户创建覆盖全部通过；
- 企业默认策略配置单独更新成功；改名、非空 `group_ids` 和删除均返回 409，拒绝请求不改变已保存配置；
- 真实腾讯云 options 返回满足 SELL/allowlist 和 CBS 系统盘约束的结果；
- 真实创建并销毁 3 台 CVM，覆盖镜像等于磁盘、小于磁盘和 `ImageSize=0`；
- `ImageSize=100`、最终磁盘 50GB 在云调用和安全组副作用前返回 400；
- Pod restart 为 0；CVM、测试安全组、StatefulSet、Service 和脚本 fixture 均完成清理或恢复。

### 时间与镜像

| 项 | 值 |
|----|----|
| 开始时间 | 2026-07-22 08:15:00 |
| 结束时间 | 2026-07-22 08:26:49 |
| 耗时 | 00:11:49 |
| Image | `keitewang-test.tencentcloudcr.com/ngc/hatchery:it-resource-policy-redesign-202607220815` |
| Build image ID | `c4f2f1eb8171` |
| Registry digest | `sha256:ec410e56ab3d3ff5f617e164344773b21e8c7bbfaac2bc7bec786bb3f1c747d1` |
| 隔离 namespace | `clawpro-test` |
| Run suffix | `6fe86982` |

镜像由当前 worktree 在远端 Docker host 构建并 push。TCR 临时密码只通过 stdin 传入 `docker login --password-stdin`，未写入仓库或本报告；push 后已执行远端 `docker logout` 并删除临时构建目录。

### 执行命令

```bash
make test \
  IMAGE=keitewang-test.tencentcloudcr.com/ngc/hatchery:it-resource-policy-redesign-202607220815 \
  BASE_BRANCH=origin/Release/2026_07_15 \
  TEST_ARGS="--run '*resource_config*' --concurrency 1 --script-timeout 45m --timeout 90m --report-dir ./test-report/resource-policy-redesign"
```

环境使用 `QUIET=1`、`NO_COLOR=1`、`IT_NAMESPACE=clawpro-test` 和 `IT_CONTAINER=hatchery`。腾讯云 AK/SK 继续由 namespace 内 Secret 注入，未传入命令行或报告。

### I01–I12 结果

| # | 场景 | 结果 |
|---|------|------|
| I01 | 六个资源策略 operation 的无 token、坏 token、普通用户 token 鉴权 | PASS；18/18 请求符合 401/403 |
| I02 | 企业默认策略闭环 | PASS；配置更新 200；改名、非空范围和删除 409；配置保持并最终恢复 |
| I03 | 未知字段兼容和已知字段非法更新原子性 | PASS；失败请求均为 400，原配置不变 |
| I04 | 列表分页、默认置顶和非法分页 | PASS |
| I05 | instance-types 真实腾讯云查询 | PASS；query 参数全覆盖，只返回 allowlist SELL |
| I06 | system-disks 真实 CVM/CBS 查询 | PASS；类型、范围、最小 50GB 和步长合法 |
| I07 | 独立策略、父子组、双向范围、树直接标记、继承和默认回退 | PASS |
| I08 | 默认/分组/用户三层创建覆盖 | PASS；真实 CVM 最终使用用户值并销毁 |
| I09 | 镜像 100GB、磁盘 50GB | PASS；400 且无云调用、SG 或 placeholder 残留 |
| I10 | 镜像等于/小于磁盘 | PASS；2 台真实 CVM 创建、反查、销毁 |
| I11 | `ImageSize=0` | PASS；1 台真实 CVM 创建、反查、销毁，磁盘未被静默修改 |
| I12 | 资源策略 API 增量覆盖 | PASS；6/6 task operations、2/2 task added params，所有资源策略 operation 参数集合完整 |

### 脚本和请求统计

| 脚本 | 结果 | Tests | HTTP |
|------|------|------:|-----:|
| `admin_resource_config/test_resource_config.py` | PASS | 6 | 39/39 |
| `admin_user_groups/test_resource_config_policy.py` | PASS | 4 | 8/8 |
| `openclaw_instance/test_exclusive_instance_create_resource_config.py` | PASS | 5 | 69/69 |
| **合计** | **3/3 PASS** | **15** | **116/116** |

覆盖收集器合并 135 frames。

### API 覆盖断言

从 `coverage-data.json` 逐帧解析并断言：

- `GET /admin/resource-policies`：存在覆盖帧，`page/page_size` 全覆盖；
- create：`name/resource_config/group_ids` 全覆盖；
- update：`id/name/resource_config/group_ids` 全覆盖；
- delete：`id` 覆盖；
- instance-types：`zone/instance_charge_type/refresh` 全覆盖；
- system-disks：`zone/instance_charge_type/instance_type/refresh` 全覆盖；
- `GET /admin/user-groups/tree` 的 `with_resource_policy` 覆盖；
- `POST /openclaw/create` 的 `resource_config` 覆盖。

当前分支相对 base 还包含两个非本任务新 operation：`GET /admin/skillhub-status`、`POST /admin/instances/batch-set-model`，本次 `*resource_config*` scoped run 未覆盖；因此全分支报告显示 6/8 new routes、2/8 new params。资源策略 I12 的目标集合为 6/6 operations 和 2/2 added params，全部通过；未把其他功能的覆盖虚报为本任务结果。

### 清理与报告

- runner cleanup 确认无待终止 CVM；
- 测试安全组已删除；
- `clawpro-test/hatchery-6fe86982` StatefulSet 和 Service 删除后再次查询均无结果；
- Pod restart check 为 0；
- 默认策略、测试策略/分组/用户和镜像 fixture 由脚本 `finally` 恢复或删除；
- 远端临时构建目录已删除，TCR 已 logout。

报告：

- `test-report/resource-policy-redesign/index.html`
- `test-report/resource-policy-redesign/coverage.html`
- `test-report/resource-policy-redesign/coverage-data.json`
- 三个脚本明细 HTML。

### 阶段结论

独立资源策略的管理、保护、分组范围、继承、真实云选项和创建链路均在隔离 EKS 环境端到端通过。下一阶段进入 Review。
