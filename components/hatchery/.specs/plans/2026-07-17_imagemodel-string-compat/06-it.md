# 集成测试

## 结论：不适用（已被 UT 覆盖）

本次改动为 shell 脚本中 jq 表达式的类型判断逻辑，已通过 `test/scripts/test_imagemodel_compat.sh` 结构化测试覆盖（10/10 通过）。集成测试需要真实 CVM 实例环境（构造历史龙虾 with 字符串格式 imageModel），本地无法运行。

## 原因分析

1. **改动性质**：仅涉及 jq 表达式内部逻辑，不涉及 API 接口、数据库、Go 代码
2. **测试覆盖**：UT 阶段已用真实 jq 命令验证所有分支（字符串/对象/不存在 × set/switch/remove）
3. **环境依赖**：集成测试需要真实 CVM 实例 + TAT 下发 + openclaw-gateway，本地无法模拟
4. **风险评估**：改动范围极小（3 个文件 +4~6 行），且 UT 覆盖完整，集成测试 ROI 低

## 建议

发布后在有历史龙虾的环境上手动验证一次：对 imageModel 为字符串格式的实例执行添加/切换/删除模型操作，确认不再报错。
