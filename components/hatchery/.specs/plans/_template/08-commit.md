# 08. Commit — 提交记录

---

## 提交前检查

> **执行顺序（强制）**：
> ① 写完本文件 → ② 更新 `00-overview.md` 状态为已完成 → ③ 确认所有 `0N-*.md` 已就绪 → ④ `git add` → ⑤ `git commit` → ⑥ `git push`

- [ ] 所有 `0N-*.md` 产物已完成
- [ ] `00-overview.md` Progress 全部勾选
- [ ] `gofmt` + `go vet` 通过
- [ ] 单元测试通过

## Commit Message

```
<type>(<scope>): <subject>
```
