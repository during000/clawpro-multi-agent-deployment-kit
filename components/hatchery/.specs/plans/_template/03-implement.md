# 03. Implement — 实现记录

---

## 关键实现细节

<!-- 只记录非显而易见的设计选择、特殊处理 -->

## 与 Plan 差异

<!-- 实现中偏离 Plan 的地方及原因，无则写「无」 -->

## 检查项

- [ ] `gofmt` 格式化通过
- [ ] `go vet ./...` 无错误
- [ ] 写接口已添加审计日志
- [ ] 数据库变更已同步 `sql/init.sql` + migration SQL
- [ ] 使用 `model.DB(r.Context())` 而非 `model.DB`
- [ ] 无硬编码密钥/配置
- [ ] 用户可见文案使用 `i18n.T()`，新增 Key 已同步 `en.go` 英文翻译
