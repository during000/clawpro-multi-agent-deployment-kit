# 04. UT — 单元测试

## 用例执行结果

| 用例/检查 | 结果 | 说明 |
|----------|------|------|
| 被排除运行时文件变化 | 通过 | 新增 `gateway.pid` + 唯一 `.hermes` 根目录告警，业务树指纹一致后执行 PUT/POST |
| 业务文件 changed 告警 | 通过 | `.hermes/config.yaml` 变化时失败，未发起上传 |
| 根目录告警但归档损坏 | 通过 | `tar tf` 失败时终止，未发起上传 |
| 正常 tar 状态 0 | 通过 | 维持原成功上传流程 |
| 非 Hermes 根目录告警 | 通过 | 兼容开关关闭时状态 1 仍失败，未发起上传 |
| 根级业务项新增 | 通过 | 指纹不一致，未发起上传 |
| 嵌套业务文件替换 | 通过 | 递归指纹不一致，未发起上传 |
| 持久化日志 | 通过 | 成功/失败阶段可见，测试凭证和上传域名未写入日志 |
| 全仓单元测试 | 通过 | 所有 Go package 通过 |
| race 定向测试 | 通过 | 迁移相关用例无 race 报告 |
| 开发构建 | 通过 | `go build -race` 成功 |
| Hermes IT Python 语法 | 通过 | `py_compile` 与 `compileall` 通过 |
| Hermes IT 契约自检 | 通过 | fake response 驱动 4 个默认测试函数通过，真实 E2E 正确按开关跳过 |
| Hermes IT TAT helper 自检 | 通过 | fake TAT invocation/task 响应验证命令等待、状态和输出解码 |
| Hermes IT SMH 鉴权自检 | 通过 | 前置检查使用 `BOOTSTRAP_ADMIN_TOKEN`，不再误用动态管理员 API Token |

## 执行命令

```bash
bash -n scripts/export_migration.sh
shellcheck scripts/export_migration.sh
GO111MODULE=on go test ./controller -run 'TestExportMigrationScript|TestBuildMigrationScript|TestAgentMigrationExcludedPaths' -count=1
GO111MODULE=on go test -race ./controller -run 'TestExportMigrationScript|TestBuildMigrationScript|TestAgentMigrationExcludedPaths' -count=1
GO111MODULE=on go test ./controller -count=1
GO111MODULE=on go test ./... -count=1
GO111MODULE=on go vet ./controller
GO111MODULE=on go build -race -o /tmp/<temporary-dir>/hatchery .
python3 test/api_md_to_openapi.py
python3 -m py_compile test/scripts/openclaw_instance/test_instance_migration_hermes.py
```

## 覆盖与未覆盖

- 已覆盖导出脚本从打包判定、归档校验、分块上传到确认上传的控制流。
- 已覆盖 Hermes/非 Hermes 范围、排除项变化、根级与嵌套业务变化、错误情况下不上传、
  失败阶段可观测和日志不泄漏测试密钥。
- 未在真实 Hermes CVM、持续活跃 Agent 面板及真实 SMH 凭证下执行端到端迁移；
  该项保留为发布前人工验收，详见 `06-it.md`。

## 静态检查备注

`GO111MODULE=on go vet ./...` 仍报告仓库已有问题：

```text
skillhubclient/client_test.go:278:8: using resp before checking for errors
```

本次改动未触及该 package；`GO111MODULE=on go vet ./controller` 通过。

2026-08-03 补集成测试后，`TestBuildMigrationScript|TestAgentMigrationExcludedPaths|TestHandleMigration`
定向 Go 回归通过。当前受控本地执行环境不允许 Bash process substitution 打开 `/dev/fd/*`，
因此本轮复跑 7 个 `TestExportMigrationScript_*` 均在日志重定向初始化处报
`/dev/fd/62: Operation not permitted`；这是执行环境限制，非断言或产品逻辑失败，原提交前的
非受限环境执行结果仍保留在上表中。尝试运行其余 controller 全量测试时，也因沙箱禁止
`httptest` 监听 `[::1]:0` 而在 `TestDoCheckSecurityGroupExists_ResponseNil` 中止；迁移定向测试
不依赖监听端口并已通过。
