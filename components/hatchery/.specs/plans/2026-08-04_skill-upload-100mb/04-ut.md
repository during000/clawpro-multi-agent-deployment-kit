# 04. UT — 单元测试

---

## 用例执行结果

> 与 Plan §测试用例 逐条对齐

| # | Plan 用例 | 测试函数 | 结果 |
|---|----------|---------|------|
| 1 | 常量值正确（100MB） | `TestSkillUploadMaxSize_Is100MB` | PASS（race） |
| 2 | 边界：恰好等于上限 → 允许 | `TestIsSkillUploadTooLarge_Boundary` | PASS（race） |
| 3 | 边界：超过 1 字节 → 拒绝 | `TestIsSkillUploadTooLarge_Boundary` | PASS（race） |
| 4 | 上传超限文案含 100MB，与 Bundle 50MB 拆分 | `TestMsgSkillUploadFileSizeTooLarge_Mentions100MB` | PASS（race） |
| 5 | FileTooLarge 注释更新 / 无 panic | `TestHandleCreateSkill_FileTooLarge` | PASS（race，弱断言，走 zip 失败路径） |

- P0：4/4 通过
- P1：1/1 通过

## 执行命令

```bash
go test ./controller/ -v -race -count=1 \
  -run 'TestSkillUploadMaxSize_Is100MB|TestIsSkillUploadTooLarge_Boundary|TestMsgSkillUploadFileSizeTooLarge_Mentions100MB|TestHandleCreateSkill_FileTooLarge'

go test ./controller/ -coverprofile=/tmp/skill-upload-100mb.cover -covermode=atomic -count=1 \
  -run 'TestSkillUploadMaxSize_Is100MB|TestIsSkillUploadTooLarge_Boundary|TestMsgSkillUploadFileSizeTooLarge_Mentions100MB|TestHandleCreateSkill_FileTooLarge'

go tool cover -func=/tmp/skill-upload-100mb.cover | rg skill_upload

# CI 增量脚本（见下方说明）
BASE_BRANCH=origin/Release/2026_07_31 bash .ci/ci-check-coverage.sh
```

## 覆盖率

| 维度 | 结果 |
|------|------|
| `isSkillUploadTooLarge` | **100.0%** |
| 本次 `skill_upload.go` 可执行增量行 | **80.0%（4/5）** |
| `controller` 包（仅跑上述用例） | 0.2%（包体量大，仅定向用例） |

增量行明细（相对工作区 `git diff`）：

| 行 | 内容 | 覆盖 |
|----|------|------|
| 26–28 | `isSkillUploadTooLarge` 函数体 | 已覆盖 |
| 125 | `if isSkillUploadTooLarge(header.Size)` | 已覆盖（false 分支，小文件） |
| 127 | `I18nError(MsgSkillUploadFileSizeTooLarge)` 返回体 | **未覆盖**（需真实 >100MB multipart） |

`i18n/keys.go` / `i18n/en.go` 为声明与 `init` 注册，无独立业务分支；文案正确性由 UT #4 断言。

## 未覆盖行

- `skill_upload.go:127`：超限时返回 `MsgSkillUploadFileSizeTooLarge` 的 true 分支。构造 >100MB multipart 成本过高；等价判定已由 `isSkillUploadTooLarge(skillUploadMaxSize+1)==true` 覆盖。增量 80% ≥ 要求 80%。

## CI 脚本说明

`BASE_BRANCH=origin/Release/2026_07_31 bash .ci/ci-check-coverage.sh` 输出 `No Go files changed in this PR. PASS`：

1. 业务改动仍在工作区，尚未 Commit，脚本按 `...HEAD` 比较看不到 diff。
2. 脚本对 `BASE_BRANCH` 再次加 `origin/` 前缀，revision 解析异常（`origin/origin/Release/...`）。

真实增量覆盖率以上表手工统计为准。
