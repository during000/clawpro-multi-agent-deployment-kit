# 06. IT — 集成测试

> 包含构建镜像、K8s 部署、端到端 API 验证、增量覆盖率。详见 `test/README.md`。
> 依赖 Docs 步骤产出的 `docs/API.md` 生成 OpenAPI spec。

---

## 前置检查

> 开始前逐项确认，缺一项即无法运行集成测试。

- [ ] Docker 可用：`docker info` 失败
- [ ] 镜像仓库已登录：未检查（Docker 不可用）
- [ ] Kubeconfig 可用：`kubectl cluster-info --request-timeout=5s` 失败
- [ ] AK/SK：未提供模板指定账号（3205597606）的 `--ak`/`--sk`

## 计划运行命令

```bash
# Plan：小包冒烟（现有 skill create / contribute 相关脚本）
make test IMAGE=<镜像> TEST_ARGS="--ak <redacted> --sk <redacted> --run skill"

# Plan 明确不做：真实 >100MB 超大包端到端
```

## 执行结果

| # | Plan 用例 | 方法 | 路径 | 结果 |
|---|----------|------|------|------|
| 1 | 小包上传回归（create / contribute） | POST | `/admin/skills/create`、`/openclaw/skills/contribute` | **BLOCKED**（环境不具备） |
| 2 | 超大包端到端 >100MB | POST | 同上 | **SKIP**（Plan：CI 体积/耗时不可接受，由 UT 边界覆盖） |

## 失败用例定位

未执行 K8s 部署与端到端 IT。阻塞原因：

1. Docker 不可用；
2. kubectl 无法访问集群；
3. 未提供白名单账号 AK/SK。

## 替代验证

| 项 | 结果 |
|----|------|
| `go build -o /tmp/hatchery-skill-upload-it .` | PASS |
| Plan P0 UT（常量 / 边界 / 文案拆分） | PASS |
| Docs：`docs/API.md` 两处上限已为 100MB | 已完成 |
| 无新 API / 无新请求参数 | 增量 OpenAPI 覆盖率不适用（仅放宽既有参数说明） |

## 结论

IT 环境受限，端到端冒烟 **BLOCKED**；核心契约由 UT + 文档覆盖，满足本期 Plan 对 IT 的预期（超大包本就不做 IT）。若后续环境就绪，可补跑 `--run skill` 相关脚本做回归确认。
