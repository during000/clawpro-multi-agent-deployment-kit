# 06. IT — 集成测试

---

## 环境与部署

- K8s：`openclaw/admin-260073873`
- 租户 identifier：`clp-l79hvbbx`
- 白名单：`passwordless-login | clp-l79hvbbx`
- 后端镜像：`keitewang-test.tencentcloudcr.com/ngc/hatchery:yutaoguo-test-passwordless-login-link-2d54a64-it1`
  - digest：`sha256:5b61cf12cd388c21fe5302a8f20234f416568ab85a5170c0b5ec4096d2a9475d`
- 前端镜像：`keitewang-test.tencentcloudcr.com/ngc/openclaw-nginx:yutaoguo-test-passwordless-login-link-c0f409e-it1`
  - digest：`sha256:3f9a0d286ba0c1a50429afa2ce030151269d50a39c0c81e4f00786a5c7bfe3a5`
- StatefulSet rollout 完成；`app`、`frontend` 均 Ready，restart count 均为 0。

## API 集成测试

脚本：`test/scripts/passwordless_login/test_passwordless_login.py`

| # | 用例 | 结果 |
|---|------|------|
| IT-01 | 管理员用户 API Token 为同租户目标用户签发链接，首次消费建立目标 Session | PASS |
| IT-02 | 同一凭证第二次消费返回 401，首次建立的 Session 保持有效 | PASS |
| IT-03 | 普通用户 API Token 调用签发接口返回 403 | PASS |
| IT-04 | 缺失 `user_id`、缺失 `token` 均返回 400 | PASS |
| IT-05 | 伪造凭证返回 401，且不创建 Session | PASS |

结果：5/5 用例通过，11/11 HTTP 请求符合预期。

## 浏览器端到端验证

| # | 场景 | 结果 |
|---|------|------|
| FE-01 | 有效 Fragment 链接自动消费、清空 Fragment、跳转首页并登录目标用户 | PASS |
| FE-02 | 缺失凭证显示稳定错误页且不请求消费接口 | PASS |
| FE-03 | 已消费链接重放显示 401 对应错误页，Fragment 已清空 | PASS |
| FE-04 | 已登录用户 A 消费用户 B 的链接后，Session 与页面身份切换为 B | PASS |
| FE-05 | 新增前端文案均使用 `t()` 标记；生成翻译 JSON 由前端 i18n 流程负责 | PASS |
| FE-06 | Aegis RUM 请求不包含 `passwordless_login_token` | PASS |

安全修复：`index.html` 在应用模块加载前转移并清除 Fragment；Aegis `urlHandler` 与 `beforeRequest` 双重脱敏 Navigation Timing 中的原始 URL。

## 增量 API 覆盖率

- 新增 operation：2
- 已覆盖新增 operation：2/2（100%）
- `POST /admin/passwordless-login-link`：覆盖 200、400、403，覆盖请求参数 `user_id`
- `POST /auth/passwordless-login`：覆盖 200、400、401，覆盖请求参数 `token`
- HTML 报告：`/tmp/passwordless-coverage.html`（本地临时产物，不提交）

## 备注

- 单次使用限制已在真实 K8s 环境验证：首次 200，重放 401。
- 接口返回 `expires_in=120`；过期拒绝由模型与 Handler UT 覆盖。按用户指令停止额外等待 125 秒的在线过期实验，不将未完成实验计为 IT 通过依据。
- `AGENTS.md` 与 `docs/openapi_base.json` 按用户要求不提交、不修改 `.gitignore`。
