# 07. Review — 代码审查

## 审查方式

- [x] AI 自动 Review
- [ ] 人工 Review

## 发现的问题

| 严重度 | 问题 | 影响 | 处理 |
|--------|------|------|------|
| 中 | 云端标签正确，但创建期本地缓存只记录默认标签 | 创建完成前的管控列表可能展示过期标签 | 缓存最终合并标签，补充两个入口回归测试并完成真实云复测 |
| 中 | RunInstances 标签错误仅按错误码映射为 400 | 用户未传标签时，失效的管理员默认标签被错误归因给调用方，并改变旧接口 500 语义 | 仅当请求传入至少一个自定义标签时返回 400；未传入时保持 500 |

| 维度 | 结论 |
|------|------|
| 正确性 | 两入口汇合到同一主流程；云端请求与本地缓存使用相同合并结果；同 key 冲突、请求标签错误状态和兼容边界有测试 |
| 安全性 | 未新增权限入口、裸 SQL、密钥或请求体日志；日志仅记录标签数量 |
| 规范性 | 两个写入口继续使用既有 `WithAudit`；API 文档和 IT 脚本同步；无数据库/i18n 变更 |
| 可维护性 | 解析、合并、注入集中到小型共享 helper，管理员端不重复实现 |
| 性能 | 无新增数据库/Tag API 调用；合并复杂度 O(custom + default) |

基线/环境事项（不由本次增量引入）：

- 全仓 `go vet ./...` 报 `skillhubclient/client_test.go:278:8: using resp before checking for errors`。
- 当前开发环境未安装 `staticcheck`。
- Kubernetes 部署编排 IT 未执行；本地 Hatchery 连接真实腾讯云的核心路径已通过，详见 `06-it.md`。

## 审查通过确认

- [x] 无高严重度未修复问题
- [x] 代码风格一致
- [x] 安全基线检查通过
