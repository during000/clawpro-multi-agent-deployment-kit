# 04. UT — 单元测试

## 用例执行结果

| 范围 | 结果 | 说明 |
|------|------|------|
| 标签 JSON 解析 | PASS | 空值、多标签、object、未知字段、尾随值、非法 JSON |
| 标签合并 | PASS | 自定义覆盖默认；自定义重复 key 保留给云端校验 |
| 标签错误 HTTP 映射 | PASS | 请求传入自定义标签时标签错误为 400；未传入时为 500 |
| 用户端 handler | PASS | 非数组 tags 在创建占位实例前返回 400 |
| 管控端 handler | PASS | tags 元素未知字段返回 400 |
| 用户端完整成功链路 | PASS | 捕获 RunInstances 请求，断言自定义+默认标签 |
| 管控端完整成功链路 | PASS | 捕获 RunInstances 请求，断言自定义+默认标签 |
| 标签缓存 | PASS | 创建成功后的缓存内容与 RunInstances 最终合并标签一致 |
| 全仓测试 | PASS | `go test ./...` 全部通过；controller 193.321s |
| vet | PARTIAL | `controller/...` 通过；全仓被既有 `skillhubclient/client_test.go:278` 挡住 |
| staticcheck | NOT RUN | 当前环境未安装命令 |

## 执行命令

```bash
GO111MODULE=on go test ./controller -run '^(TestParseCreateInstanceTags|TestMergeCreateInstanceTags_.*|TestCreateInstanceTagItemsForCache_MatchesMergedRunInstancesTags|TestCvmRunInstancesHTTPStatus|TestHandleCreateInstance_InvalidCustomTags|TestAdminCreate_TagsRejectUnknownNestedField|TestHandleCreateInstance_RunInstancesSuccess_WriteCtxPath|TestAdminCreate_FullSuccess)$' -count=1
GO111MODULE=on go test ./...
GO111MODULE=on go vet ./...
GO111MODULE=on go vet ./controller/...
GO111MODULE=on staticcheck ./...
```

## 覆盖率

聚焦用例的包级覆盖率为 1.9%（只运行本需求相关用例，包整体很大）。本需求新增函数：

| 函数 | 覆盖率 |
|------|--------|
| `parseCreateInstanceTags` | 91.7% |
| `mergeCreateInstanceTags` | 100.0% |
| `createInstanceTagItemsForCache` | 85.7% |
| `applyCreateInstanceTags` | 80.0% |
| `cvmRunInstancesHTTPStatus` | 100.0% |

## 未覆盖行

- `parseCreateInstanceTags` 的防御性“第二次 Decode 成功但 err=nil”分支不可由标准 JSON 解码器正常触发。
- `applyCreateInstanceTags` 的“最终标签为空、不覆盖模板请求”分支由全仓既有无默认标签创建用例间接覆盖；本次聚焦 coverage 未单独执行该用例。
