# 06. IT — 集成测试

> 包含构建镜像、K8s 部署、端到端 API 验证、增量覆盖率。详见 `test/README.md`。
> 依赖 Docs 步骤产出的 `docs/API.md` 生成 OpenAPI spec。

---

## 前置检查

> 开始前逐项确认，缺一项即无法运行集成测试。

- [x] Docker 可用：远程构建主机已成功构建 Hatchery 镜像
- [x] 镜像仓库已登录：测试镜像已推送至 TCR
- [x] Kubeconfig 可用：已成功更新 `openclaw/admin-260073873` StatefulSet 并完成 rollout
- [x] AK/SK：历史真实 API 验证时已通过测试参数注入；文档不记录凭据值

## 运行命令

最终提交如需重跑，使用当前提交重新构建的镜像执行：

```bash
make test IMAGE="${FINAL_IMAGE}" \
  TEST_ARGS="--run openclaw_instance/test_admin_instance_create.py --ak ${TENCENT_SECRET_ID} --sk ${TENCENT_SECRET_KEY}"
```

历史部署镜像为：

```text
keitewang-test.tencentcloudcr.com/ngc/hatchery:yutaoguo-test-feature-clawpro-agent-admin-create-assign-99504b5
```

该镜像验证发生在最终 rebase 和文件收敛前；当前最终提交未重新构建和部署，因此不能把历史部署结果表述为最终提交的端到端验证。

## 执行结果

| # | 用例 | 方法 | 路径 | 结果 |
|---|------|------|------|------|
| 1 | 历史版本管理员创建套件：8 tests / 15 requests | POST/GET | `/admin/instances/create` 及当时的预设状态路径 | PASS |
| 2 | 最终脚本：鉴权 | POST | `/admin/instances/create` | NOT RUN（最终提交） |
| 3 | 最终脚本：非法 JSON 和未知字段 | POST | `/admin/instances/create` | NOT RUN（最终提交） |
| 4 | 最终脚本：必填字段 | POST | `/admin/instances/create` | NOT RUN（最终提交） |
| 5 | 最终脚本：完整嵌套请求在 CVM 前失败且 secret 不回显 | POST | `/admin/instances/create` | NOT RUN（最终提交） |
| 6 | 最终脚本：方法限制 | GET | `/admin/instances/create` | NOT RUN（最终提交） |

当前 `test_admin_instance_create.py` 保留 5 个不会申请计费 CVM 的验证函数；配置了非管理员 token 时最多发出 10 次 HTTP 请求。脚本语法已检查。最终提交的行为验证证据以聚焦 Go 测试和 `go test ./...` 为准，不能替代尚未执行的最终镜像 IT。

## 失败用例定位

最终提交没有执行这 5 个 Python 用例，因此没有可归类为 PASS 或 FAIL 的最终结果；当前状态统一记录为 `NOT RUN`。必须基于最终提交构建并部署新镜像后，才能形成最终端到端结论。
