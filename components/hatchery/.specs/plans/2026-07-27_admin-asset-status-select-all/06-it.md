# 06. IT — 集成测试

> 包含构建镜像、K8s 部署、端到端 API 验证、增量覆盖率。详见 `test/README.md`。
> 依赖 Docs 步骤产出的 `docs/API.md` 生成 OpenAPI spec。

---

## 前置检查

- [x] 远端 Docker host 可用
- [x] TCR 登录成功（临时凭证仅通过 stdin 传递）
- [x] Kubeconfig、namespace、StatefulSet、container 环境变量可用
- [x] StatefulSet 策略为 `RollingUpdate partition=0`
- [x] 隔离 IT 编排器成功创建测试实例、资源和清理资源

## 构建与部署

- 镜像：`keitewang-test.tencentcloudcr.com/ngc/hatchery:yutaoguo-test-feature-admin_asset_status_select_all-87427ff`
- Digest：`sha256:29997d82bce94109c1720c8399f25a85c8356a0c27bb6a0e55c4d926b12b76ed`
- StatefulSet：`openclaw/admin-260073873`，container=`app`
- Rollout：1/1 Pod 更新完成
- Pod：`admin-260073873-0`，`Running`，app ready=true，restart=0
- 健康检查：`GET /admin/config` 返回 HTTP 2xx 且 JSON 可解析

### 2026-07-28 插件卸载全选追加部署

- 镜像：`keitewang-test.tencentcloudcr.com/ngc/hatchery:yutaoguo-test-feature-admin_asset_status_select_all-87427ff-wd20260728175229`
- Digest：`sha256:c9404ef0e54a6b7b17e1273c79669504b1f7c19054cfd629be4f3ffe16181bdd`
- StatefulSet：`openclaw/admin-260073873`，container=`app`
- Rollout：1/1 Pod 更新完成
- Pod：`admin-260073873-0`，`Running`，app ready=true，restart=0
- 按用户要求，共享环境本轮不执行健康检查或业务 IT，不创建/修改测试资源；仅执行本地 UT。

## 定向脚本

```bash
make -s test \
  IMAGE=keitewang-test.tencentcloudcr.com/ngc/hatchery:yutaoguo-test-feature-admin_asset_status_select_all-87427ff \
  TEST_ARGS="--run 'test_admin_*_redelivery_after_reinstall.py' --concurrency 1"
```

首次运行被本机默认 Python 3.14 缺少 `requests` 阻断，K8s 资源已自动删除；切换到已具备依赖的 Python 3.13 后重跑成功。

## 执行结果

| # | 用例 | 方法 | 路径 | 结果 |
|---|------|------|------|------|
| 1 | 技能全选新参数 | `select_all/statuses/group_ids` | `POST /admin/skills/distribute` | PASS：进入目标解析，零候选返回 400 |
| 2 | 插件全选新参数 | `select_all/statuses/group_ids` | `POST /admin/plugins/distribute` | PASS：进入目标解析，零候选返回 400 |
| 3 | MCP 全选新参数 | `select_all/statuses/group_ids` | `POST /admin/mcp/distribute` | PASS：进入目标解析，零候选返回 400 |
| 4 | 分组隔离 | 需要至少两个候选实例 | 三类 distribute | BLOCKED：候选列表均为 0 |
| 5 | installed 重复下发 | 需要已安装实例 | 三类 distribute | BLOCKED：候选列表均为 0 |
| 6 | 新参数路由覆盖 | 真实 HTTP 请求 | 三类 distribute | PASS |
| 7 | 显式 ID 冒烟兼容 | `instance_ids=[999999]` | `POST /admin/mcp/distribute` | PASS：旧模式完成解析并按无有效实例返回 400 |
| 8 | 企业技能基本下发/重装后再次下发 | 现有 IT 脚本 | `skill/test_admin_skill_redelivery_after_reinstall.py` | PASS |
| 9 | 企业插件基本下发/重装后再次下发 | 现有 IT 脚本 | `plugin/test_admin_plugin_redelivery_after_reinstall.py` | PASS |
| 10 | 企业 MCP 基本下发/重装后再次下发 | 现有 IT 脚本 | `mcp/test_admin_mcp_redelivery_after_reinstall.py` | PASS |

## 失败用例定位

- 三个基本下发脚本全部通过，Pod restart check 无重启；测试实例、用户、安全组、StatefulSet 和 Service 已清理。
- 用户指定完整全选 IT（真实 `select_all` 命中、分组隔离、installed 重复下发、OpenAPI 增量覆盖）后续再做；当前常驻环境候选列表为 0，只完成了新参数零目标负路径。
- 端口转发已停止；远端构建临时目录已删除。
